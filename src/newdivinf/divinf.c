/*
** EPITECH PROJECT, 2017
** div inf
** File description:
** div inf
*/

char	*r_divinf(char *s1,char *s2,char *n,char *q)
{
	?	qtmp = 0;
	
	if (!*s1)
		return (0);
	while (compare_str(n, s2) < 0 && *s1) {
		n[length?] = *s1;
		s1++;
	}
	while(compare_str(n, s2)) >= 0 {
			n = subinf(n, s2);
			qtmp++;
		}
	if (*n1 == '0')
		*n1 = '\0';
	q[length?] = q - qtmp + '0';
	return(r_divinf(s1, s2, n, q));
}

		
